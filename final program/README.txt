Titles with a 1 in the front are the important programs, unbolded titles dont work or are test files.

1 ApointmentGUI- makes the appointments using a unique uuid to populate a log file

1 LogTest- Is used to read the appointment log files generated by apointmentGUI

ReadLogTest- a test program that is the foundation for trlogtest

1 Trlogtest- A program that creates logs, reads them and puts them in the logs folder, this was supposed to be a more
upgraded version of apointmentgui that we would intergrate sound into but I didnt have enough time
to finish the program

TimerTest- a test program where we tried to get the timerfunction to work, not currently working

1 testos- the python file that supports the .os functions in Trlogtest